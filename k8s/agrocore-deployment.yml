# =========================
# DEPLOYMENT
# =========================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: agronotificacao
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agronotificacao

  template:
    metadata:
      labels:
        app: agronotificacao

    spec:
      containers:
        - name: agronotificacao
          image: euturial/agronotificacao:latest
          imagePullPolicy: Always

          ports:
            - containerPort: 8080

---
# =========================
# SERVICE (ClusterIP)
# =========================
apiVersion: v1
kind: Service
metadata:
  name: agronotificacao
spec:
  type: ClusterIP

  selector:
    app: agronotificacao

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080

---
# =========================
# INGRESS
# =========================
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: agronotificacao-ingress

  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /

spec:
  ingressClassName: nginx

  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: agronotificacao
                port:
                  number: 80
